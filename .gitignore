# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.env
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Config with sensitive info
config.json
sender_config.json

# Logs
logs/
*.log

# 运行时数据目录（数据库 / 日志 / 断点，本地生成不提交）
data/

# 转发去重指纹库（本地生成，不提交）
forward_dedup.db
forward_dedup.db-wal
forward_dedup.db-shm
tmp_forward_dedup_test.db*
# 转发去重运行日志（默认关闭，LOG_ENABLED=True 时才写）
forward_dedup_log.jsonl
tmp_forward_dedup_*.json*

# Personal photos
imgs/dress/
